# dependencies
node_modules/

# build output
dist/
build/

# captured session data — your local SQLite event store (+ WAL/SHM sidecars).
# Personal activity data, never commit it.
events.db
events.db-wal
events.db-shm
*.ndjson

# runtime logs
*.log

# TypeScript incremental build cache
*.tsbuildinfo

# macOS / editor cruft
.DS_Store
.idea/
.vscode/
*.swp

# local env / secrets
.env
.env.*
